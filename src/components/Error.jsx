
const Error = ({children}) => {
    return (
        <div className="bg-red-500 p-3 uppercase font-bold text-white text-center mb-5 rounded-md">
            {children}
        </div>
    )
}

export default Error
